<%= form_for([:admin, @preferencia]) do |f| %>
<%= render 'shared/error_messages', :object => f.object %>

  <div>
    <%= f.label :nome %>
    <%= f.text_field :nome, :class => "text" %>
  </div>

  <div>
    <%= submit_tag "Criar", :class => "send"  %>

  </div>
<% end %>