<%= form_for([:admin, @germinacao_taxa]) do |f| %>
<%= render 'shared/error_messages', :object => f.object %>

  <div>
    <%= f.label :taxa %>
    <%= f.text_field :taxa %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>